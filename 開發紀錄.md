# Alto Fingering Player · 開發紀錄

**建立日期：** 2026-08-31  
**最近續作：** 2026-09-01  
**狀態：** 可離線本機單頁 App（YAS-280／YDS-150；準備／練習；對音；本機樂譜清單；.mxl）。已發布 GitHub Pages。  
**路徑：** `/home/sweet/alto-fingering-player/`  
**GitHub：** https://github.com/ergeargwer/alto-fingering-player  
**試用：** https://ergeargwer.github.io/alto-fingering-player/  
**用途：** 保存本討論串的產品決定、指法表、檔案清單、驗證結果，方便之後接續改。

相關既有文件（不要混在一起）：

| 檔案 | 關係 |
|------|------|
| `/home/sweet/Music/scores/中音薩克斯_樂譜與跟練指南.md` | 樂譜製作與 MuseScore 跟練 |
| `/home/sweet/INSTALL_SAX_FINGERING.md` | MuseScore 4.7.4 指法外掛安裝（Saxy 字型） |
| 本專案 `README.md` | 給使用者的短說明 |

**不是** PlayThatSheet 的複製。UI、指法 SVG、示範曲都是自製。沒有後端、登入、網路 API。

---

## 1. 為什麼做

聽眾：中音薩克斯初學者，想跟吹但不想讀唱名／五線。

需要：

1. 開頁就能吹（內建示範曲）
2. 本機載入 MusicXML／JSON
3. 大張薩克斯指法圖跟著目前的音亮鍵
4. 速度可調、可循環、可看下一音
5. 介面用**繁體中文**，音名仍用英文（G4、B♭4）

本機已有一批 Alto 記譜 MusicXML（`/home/sweet/Music/scores/`），適合直接丟進這個播放器跟吹。

---

## 2. 怎麼開

專案是靜態檔，用瀏覽器開即可：

```
/home/sweet/alto-fingering-player/index.html
/home/sweet/alto-fingering-player/fingering-test.html
```

或：

```bash
python3 -m http.server 8765 --directory /home/sweet/alto-fingering-player
# 然後開 http://127.0.0.1:8765/
```

取樣 MP3 用 `fetch`，請用 http 伺服器或 GitHub Pages，不要依賴 `file://`。

空白鍵＝播放／暫停。速度與顯示模式（指法／音名／兩者）存在 `localStorage`：

- `altoFingeringPlayer.tempo`
- `altoFingeringPlayer.displayMode`
- `altoFingeringPlayer.countIn` / `slowStart` / `voice` / `muted` / `bb`（`side` 或 `bis`）
- `altoFingeringPlayer.instrument`（`yas-280` 或 `yds-150`）

準備／練習模式**不**寫進 localStorage；重新整理回到準備。

本機樂譜資料夾（Chromium）記在 IndexedDB `altoFingeringPlayer`，只存目錄 handle，不存樂譜內容。

---

## 3. 檔案清單

| 檔案 | 角色 |
|------|------|
| `index.html` | 播放器頁 |
| `fingering-test.html` | 記譜鋼琴指法測驗 |
| `styles.css` | 深色練習室風格、響應式 |
| `fingerings.js` | 共用記譜指法表＋YAS-280／YDS-150 兩套原創 SVG |
| `parser.js` | 瀏覽器內 MusicXML／.mxl／JSON |
| `demo.js` | 內建 Practice Riff |
| `library.js` | 本機資料夾清單（File System Access／webkitdirectory） |
| `tuner.js` | 對音：YIN 音高、實音→記譜 +9，不上傳 |
| `app.js` | 播放排程、UI、載檔 |
| `sax-audio.js` | 實音取樣／簧片合成、殘響、靜音 |
| `samples/` | FluidR3 Alto 六音 MP3（Eb3–G5）＋ LICENSE.txt |
| `practice-riff.musicxml` | 示範曲 MusicXML（測解析） |
| `practice-riff.json` | 同上 JSON |
| `selftest.html` | Chromium 自動檢查 |
| `README.md` | 使用說明（GitHub） |
| `LICENSE` | 程式 MIT |
| `開發紀錄.md` | 本檔 |

沒有 npm、沒有打包、沒有 ES module（用 `<script>` 順序載入）。音檔需要 http 來源。

---

## 4. 音樂規則（之後改碼請維持）

- 樂器：中音薩克斯 **Eb**。指法一律用**記譜音高**。
- 示範曲已是 Alto 記譜，不要再移調。
- 勾選「此檔為實音」：載入音上移大六度（**+9 半音**）再查指法。
- 音域：記譜 **B♭3（MIDI 58）～ F♯6（MIDI 90）**。超出顯示「音域外」。
- 播放聲音是**實音**（記譜 −9），圖上音名是記譜。
- 只取第一條單旋律：忽略歌詞、力度、和弦加音；`<chord/>` 的後續音丟掉。
- 休止：不亮鍵，顯示「休止」。

### 示範曲 Practice Riff

- 4/4、預設 90 BPM、12 小節
- 只用記譜 G4 A4 B♭4 C5 D5 E♭5 F5 G5
- 有四分、八分，以及兩小節全休止（測 REST）
- 第 7–8 小節是休止

---

## 5. 指法表（已對過、請不要混用兩套）

來源（同一套「基本」表，不要再東拼西湊）：

- Woodwind Fingering Guide Basic  
  https://www.wfg.woodwind.org/sax/sax_bas_1.html  
  https://www.wfg.woodwind.org/sax/sax_bas_2.html  
- 交叉核對 Tunable alto written chart

**第一輪**依產品規格用 Bis B♭（L1 + Bis）。  
**第二輪**使用者要求對「標準初學表」修正：WFG **Basic 的 B♭ 是側 B♭**（L1 L2 + 側B♭）。Bis 改為備用，只在測驗頁標出。

| 記譜 | 按下 | 備註 |
|------|------|------|
| 低 B♭ | 六孔 + 低B♭ + 低C | 右小指低 C 匙要按 |
| 低 B | 六孔 + 低B + 低C | |
| 低 C | 六孔 + 低C | |
| 低 C♯ | 六孔 + 低C♯ + 低C | |
| D | 六孔 | 無粉紅指 |
| E♭ | 六孔 + 右小指 E♭ | |
| E | 左123 + 右12 | |
| F | 左123 + 右1 | |
| F♯ | 左123 + **右2（叉指）** | 不用輔助 F♯ 縫 |
| **G4** | **左123，無八度** | 必對 |
| G♯ | 左123 + G♯ | |
| **A4** | **左12** | 必對 |
| **B♭4／B♭5** | **左12 + 側B♭** | 基本（預設）；播放器與測驗頁可改 Bis＝L1+Bis |
| B | 左1 | |
| **C5** | **只按左2** | 不是六孔、也不是長笛 C |
| C♯5 | 全開 | |
| D5 起 | 同上再加八度 | |
| D6 | 八度 + 掌 D | |
| E♭6 | 八度 + 掌 D + 掌 E♭ | |
| E6 | 再加側 E | |
| F6 | 再加掌 F | |
| F♯6 | 八度 + 三掌鍵 + 高 F♯ | WFG 基本表只到 F6 |

鍵 id（SVG `data-key`，不要改名除非連圖一起改）：

`octave, frontF, L1, Bis, L2, L3, Gs, lowCs, lowB, lowBb, palmD, palmEb, palmF, highFs, R1, R2, R3, Fs, sideBb, sideC, sideE, lowEb, lowC`

游標／點擊鍵會浮出中文全名（`Fingerings.bindKeyTips`）。

---

## 6. 播放器行為

- Web Audio：**FluidR3 Alto 取樣**（六音變速）為預設；「後備合成音」為簧片 bandpass + 氣聲。方波只留給預備拍的拍點，不當主音色。
- 指法／畫面＝記譜；喇叭＝實音（記譜 −9）。
- 視覺用 `requestAnimationFrame`；音符用 `AudioContext.currentTime` 前瞻排程。
- 亮鍵維持該音時值；目前音有縮小的時值條。
- 整曲循環。改速度**不重頭**（除非已停止）。
- 空白鍵播放／暫停。
- **預備拍**：播放前先打一小節拍點（第一拍加重）。暫停後再按播放不重打；停止後再播放會重打。
- **慢速起**：第一遍 A–B（或全曲）用設定速度的 70%，接下去原速。
- **A–B**：進度條兩端把手，循環片段。預設全曲。
- **最少顯示 300ms**：十六分音符音還是照時值響，指法／音名至少留 0.3 秒。
- **B♭ 指法**：預設側 B♭；選 Bis 會重編目前曲的亮鍵，不重頭。

顯示模式：

- **指法**（預設）：大指法圖 + 下一音虛線預覽（40% 透明、虛線描邊）
- **音名**：超大 C–D–E
- **兩者**

---

## 7. MusicXML 解析（瀏覽器）

讀的最小子集：

- `<note>` `<pitch>` `<step>` `<alter>` `<octave>` `<duration>` `<voice>` `<rest>`
- `<attributes><divisions>`
- `<sound tempo="…">` 或 `<metronome><per-minute>`

多聲部：列出 `part-list` 的聲部，**聲部**下拉選單切換（會用同一份 XML 重解析，不需重選檔）。只有一個聲部時隱藏下拉。

壓縮 `.mxl`：當 ZIP 讀 `META-INF/container.xml` 的 rootfile，否則取第一個 `.xml`／`.musicxml`。只支援 store 與 deflate（`DecompressionStream`）。ZIP64 請改匯出 `.musicxml`。

失敗時：紅字說明，**示範曲仍可播**。

曾用本機 `/home/sweet/Music/scores/中文歌_入門/01_月夜愁.musicxml` 試過：標題「月夜愁」、36 個旋律音。

JSON 形狀見 `README.md`。`dur` 以四分音符為 1 拍。

---

## 8. UI 決定

- 繁體中文標籤；音名維持 G4、B♭4。
- 深色練習室、大按鈕。
- 直向手機：指法在上、音名條在下，底欄仍要看得到播放。檔案列改單列橫向捲，本機樂譜清單預設收合，讓指法圖更大。
- 橫向矮螢幕：藏導覽條／頁尾／本機清單，指法圖優先。
- 六音導覽：「按亮的鍵、吹到下一格」（G A B♭ C D E♭）。
- 頁尾說明：檔案只在本機、不上傳。

Grid 用 **named areas**（top / banner / onboard / stage / transport / files / library / footer）。`banner` 若 `display:none` 仍占自己的 area，避免 stage 搶走 `1fr` 把播放列擠掉。這是實測過的坑。

直向檔案列必須給 grid 子項 `min-width: 0`，否則 `flex-wrap: nowrap` 的 min-content 會把整頁撐超寬，指法圖／時鐘被裁到畫面外（2026-09-01 踩過）。顯示模式鈕釘在檔案列右側，左側橫向捲。

---

## 9. 指法測驗頁

`fingering-test.html`

- 記譜鋼琴 B♭3–F♯6（不是實音鍵盤）
- 另有半音按鈕列，方便逐音點
- 點音 → 左圖必須對上初學表
- B♭ 預設側鍵；頁上可改 Bis，圖立刻換
- 鍵名同樣可 hover／tap

這頁是核對指法的主工具，改 `fingerings.js` 之後先開這頁點過 G4、A4、B♭4、C5、D5、D6、F6。

---

## 10. 驗證

### 2026-08-31

`selftest.html` 在 Chromium headless 跑過，標題 `SELFTEST PASS`，包括：

- G4 / A4 / 側B♭ / C5 只左2 / D5 加八度 / 低B♭ / 叉指 F♯ / 掌鍵 F6
- 示範曲 12 小節＝48 拍、兩小節全休止
- Mini MusicXML 時值、休止、和弦加音略過
- 雙聲部選 P2、`<sound tempo>`、`<per-minute>`
- `practice-riff.musicxml` 整首

另用 Chromium 截圖核對桌面 1280×900、手機橫 844×390、直 390×844。

### 2026-09-01

`selftest.html` 再跑，標題 `SELFTEST PASS`。新增：

- B♭ 切 Bis 後 Bb4＝L1+Bis、Bb5＝八度+L1+Bis；G4 不變；切回側鍵還原
- 本機清單資料夾排序：中文歌_入門 → 動畫主題 → Jazz_Standards
- 自製 store ZIP `.mxl`（含 `META-INF/container.xml`）→ 標題 Mini、source＝`mxl`
- `CompressionStream("deflate-raw")` 壓的 `.mxl` 同樣可解析

額外（不進倉庫，`preview/` 已 gitignore）：

- Python `zipfile.ZIP_DEFLATED` 的 `practice-riff.mxl` → 標題 Practice Riff、48 拍
- `/home/sweet/Music/scores/中文歌_入門/01_月夜愁.musicxml` → 標題「月夜愁」、36 個旋律音

截圖再核對：桌面 1280×900、直向 390×844（檔案列單列、指法／音名釘右側、清單收合）、橫向 844×390、指法測驗頁（含 B♭ 下拉）。

稍後同日再跑 `selftest.html`：`SELFTEST PASS`，含 YIN（concert B♭3 → 記譜 G4）、YAS 的 A3 音域外、YDS 的 A3＝六孔＋低 A、G4／C5 兩套樂器相同。截圖：準備 YAS／YDS、練習 1fr 2fr、測驗頁。

自動測試**沒有**真的聽 Web Audio（headless 無手勢）；播放請本機開頁按一次。

---

## 11. 刻意沒做

- 不上傳伺服器、不登入、不抄 PlayThatSheet 素材或曲庫、不抓遠端樂譜
- 不把 `/home/sweet/Music/scores/` 的曲子打進公開倉庫
- 不描 Yamaha 手冊圖；YDS-150 面板是自繪 SVG
- 不自動偵測「這份 XML 是實音還是記譜」——要使用者勾選
- 不打包完整 GM 庫（只六音 Alto 取樣）
- 高 F♯ 以上 altissimo 不保證
- MuseScore 外掛／Saxy 字型是另一條線（見 `INSTALL_SAX_FINGERING.md`），本 App 的 SVG 是獨立圖，沒有用那套字型

---

## 12. 之後若要續作

- WebMidi（外接鍵盤對指法）
- 記住最近打開的幾首（現在 Chromium 會記住整個資料夾）

---

## 13. 修訂

| 日期 | 說明 |
|------|------|
| 2026-08-31 | 初版播放器：示範曲、原創指法 SVG、Web Audio、MusicXML／JSON、localStorage |
| 2026-08-31 | 指法改對 WFG 基本表（B♭＝側B♭）；鍵名 hover／tap；指法測驗頁；多聲部下拉；介面改繁中 |
| 2026-08-31 | 練習教練層：預備拍一小節、右側下一音卡、進度條 A–B 把手、慢速起 70%→100%、目前音至少顯示 300ms。無遠端曲庫。 |
| 2026-08-31 | 主音色改為 FluidR3 Alto 取樣（MIT，六音 MP3 本機 `samples/`，實音＝記譜−9）。後備簧片合成音；靜音不關游標。不再用方波當主音色。 |
| 2026-08-31 | 更新 README／開發紀錄；程式 MIT；發布 GitHub `ergeargwer/alto-fingering-player`，並開 GitHub Pages。 |
| 2026-08-31 | README 改為產品介紹：對象、設計理念、功能表、畫面截圖、逐步操作說明。截圖在 `docs/screenshots/`。 |
| 2026-09-01 | 本機樂譜清單（選資料夾、分組、搜尋；Chromium 記 handle）；B♭ 側鍵／Bis 切換；解析壓縮 `.mxl`；直向手機檔案列單列橫捲、清單預設收合。直向 `nowrap` 曾把整頁撐超寬，已用 `min-width: 0` 修好。 |
| 2026-09-01 | 準備／練習兩模式（body.prepare / body.practice）。開始練習後 1fr 2fr（指法 1/3、音名條 2/3），<900px 直向改上下堆。對音面板（YIN、本機麥克風、記譜＝實音+9）。停止／暫停不離開練習。音名條格子樣式未改。 |
| 2026-09-01 | 雙樂器：YAS-280（預設原聲圖、B♭3–F♯6、麥克風對音）／YDS-150（原創直列 1–6＋Oct／低A、示範音對音、提示選 A.xx Alto）。共用記譜指法表；切換不重載樂譜。 |
| 2026-09-01 | 更新 README／開發紀錄／截圖；commit 並推 GitHub Pages。 |

*改指法只改 `fingerings.js` 的 `BY_MIDI`，並同步 `selftest.html`。改完先開測驗頁點過關鍵音。*

---

## 14. 開發歷程（依討論串，方便接續）

這專案是兩天做出來的本機單頁 App。下面按討論串的決定寫，方便之後接續改。

### 2026-08-31 · 從零到可跟吹

**起因：** 本機已有一批 Alto 記譜 MusicXML（`/home/sweet/Music/scores/`），以及 MuseScore 4.7.4 的 Saxy 指法外掛（見 `INSTALL_SAX_FINGERING.md`）。外掛是把圖印在譜上；還缺一個「開頁看亮鍵就能吹」的播放器。明確**不是**抄 PlayThatSheet。

**產品約束（之後改碼請維持）：**

- 靜態檔、無後端、無登入、無公開曲庫、不上傳使用者的譜
- 繁中介面；音名用 G4、B♭4
- 指法＝記譜音；喇叭＝實音（記譜 −9）
- 「此檔為實音」才 +9，不自動猜
- 不打包完整 GM；不解析 `.mxl`（當天）；altissimo 不保證

**當天做成的層：**

1. **骨架：** `index.html` + `styles.css` + `app.js`。named grid：top / banner / onboard / stage / transport / files / footer。內建 Practice Riff（4/4、90 BPM、12 小節、第 7–8 小節全休止）。
2. **指法：** `fingerings.js` 原創 SVG。第一輪產品規格用 Bis B♭；使用者要求對「標準初學表」後改成 WFG Basic **側 B♭**（L1 L2 + 側B♭），Bis 只當備用寫在測驗頁。必對：G4＝左123 無八度、C5＝只左2、D5 起加八度。
3. **解析：** `parser.js` 瀏覽器內讀 MusicXML／JSON 最小子集；多聲部下拉；失敗紅字、示範曲仍可播。用《月夜愁》試過。
4. **練習教練：** 預備拍一小節、下一音卡、A–B 把手、慢速起 70%→100%、目前音至少顯示 300ms。
5. **音色：** 先用方波當主音，後改 FluidR3 Alto 六音 MP3（MIT，`samples/`）。方波只留預備拍。靜音不關游標。
6. **測驗頁：** `fingering-test.html` 記譜鋼琴 B♭3–F♯6。
7. **發布：** MIT；GitHub `ergeargwer/alto-fingering-player`；GitHub Pages。README 改成產品介紹，截圖在 `docs/screenshots/`。

**當天留下的續作清單：** 本機樂譜清單、Bis／側B♭ 切換、WebMidi、解析 `.mxl`、直向檔案列收成一列。

### 2026-09-01 · 接續清單（WebMidi 沒做）

使用者：「讓我們繼續 alto-fingering-player」。依 §12 清單做了四項，沒有把曲子打進倉庫。

| 項目 | 作法 |
|------|------|
| 本機樂譜清單 | 新檔 `library.js`。「選擇樂譜資料夾」→ Chromium `showDirectoryPicker`（IndexedDB 只存目錄 handle）或 `webkitdirectory` 後援。依子資料夾分組、可搜尋。列出 `.musicxml`／`.xml`／`.mxl`／`.json`。不是公開曲庫。 |
| B♭ 側／Bis | `Fingerings.setBbStyle("side"\|"bis")`。播放器與測驗頁下拉；改了重編亮鍵、不重頭。`localStorage` 鍵 `altoFingeringPlayer.bb`。預設仍側 B♭。 |
| `.mxl` | `parser.js` 讀 ZIP EOCD／central directory；`META-INF/container.xml` 的 rootfile，否則第一個 xml。store 與 `deflate-raw`（`DecompressionStream`）。ZIP64 請改 `.musicxml`。`SongParser.parseFile(buffer, name)`。 |
| 直向檔案列 | 檔案列單列橫捲；顯示模式釘右側；本機清單預設收合；橫向矮螢幕藏清單。 |

**直向坑（已修）：** 檔案列 `nowrap` 後，grid 子項預設 `min-width: auto`，min-content 把 `#app` 撐得比視窗還寬。overflow hidden 只看得到左邊：時鐘、薩克斯圖、音名模式鈕都像「消失」。修法：`#app` 與各 area `min-width: 0`；`.files-scroll` 自己橫捲。

**怎麼開（9/1 當下）：**

```bash
python3 -m http.server 8765 --directory /home/sweet/alto-fingering-player
```

- 本機播放器：http://127.0.0.1:8765/
- 本機測驗：http://127.0.0.1:8765/fingering-test.html
- 線上：https://ergeargwer.github.io/alto-fingering-player/

**9/1 稍後 · 準備／練習＋對音（iPad／筆電）：**

- `body.prepare`（預設，重新整理回來）／`body.practice`（按「開始練習」）。停止／暫停不離開練習，頂欄「返回準備」。
- 練習：頂欄薄（曲名、記譜大字、實音、時鐘、返回、播放）；主舞台 `1fr 2fr`；&lt;900px 直向改上 32vh 指法、下音名條。隱藏導覽六卡、檔案列、頁尾、下一音大海報、音源、A–B／靜音／上一音下一音。音名條格子樣式未改。
- 準備：原工具都在；新增「對音」（`tuner.js` YIN）。麥克風本機、練習模式強制關閉。目標記譜 G4＝實音 B♭3。|cents|&lt;25 綠、25–50 黃、不同音紅字。穩定 300ms 才改判定。
- 準備畫面 `#app` 可直向捲，避免對音把六卡擠掉。`?ui=practice` 僅供截圖／測試。

**9/1 再稍後 · 雙樂器 YAS-280／YDS-150：**

- 準備畫面必選「我的樂器」，`localStorage` 鍵 `altoFingeringPlayer.instrument`。切換只重繪 SVG、重編亮鍵，不重載樂譜。
- 共用 `BY_MIDI`：G4＝L1 L2 L3、C5＝只 L2、D5＝八度＋六孔。YAS 音域 B♭3–F♯6，無低 A、無高 G。YDS 另有低 A（記譜 A3＝六孔＋`lowA`）。
- YDS 圖是原創直列面板（鍵 1–6＝L1–L3／R1–R3、Oct、低 A、側鍵／掌鍵／小指），**不是**描 Yamaha 手冊。
- 對音：YAS 用麥克風；YDS 預設不開麥，按鈕改「播放示範音」，標「YDS 請用耳機對示範音」。畫面上提示「樂器請選 A.xx Alto 音色」。
- 練習頂欄顯示目前型號。測驗頁也可切樂器。

**9/1 沒做：** WebMidi；記住最近打開的幾首（現在 Chromium 會記住整個資料夾）。

**9/1 發布：** 更新 README 截圖（準備／練習／YDS）；push `main` → GitHub Pages。

**之後改碼：** 改指法只動 `fingerings.js` 的 `BY_MIDI` 並跑 `selftest.html`、開測驗頁點 G4 A4 B♭4 C5 D5 D6 F6。樂譜繼續本機讀，不要做成公開曲庫。
